{
  "name": "imobiturbo-website",
  "version": "1.0.0",
  "description": "Local copy of imobiturbo.com.br",
  "main": "index.html",
  "scripts": {
    "dev": "npx serve .",
    "build": "node build.js",
    "build:pages": "node build-pages.js",
    "test": "node --test tests/*.test.js",
    "deploy": "node build.js && node deploy.js",
    "deploy:pages": "npm run build:pages && npx wrangler pages deploy .cloudflare-pages --project-name imobiturbo-website",
    "deploy:lp-simulador": "npx wrangler pages deploy lp-simulador --project-name imobiturbo-lp-simulador",
    "deploy:lp-simulador-route": "npx wrangler deploy --config wrangler.lp-simulador.jsonc",
    "deploy:lp-pages": "npx wrangler pages deploy lp-pages --project-name imobiturbo-lp-pages",
    "deploy:lp-pages-route": "npx wrangler deploy --config wrangler.lp-pages.jsonc",
    "deploy:xpertinformatica": "npx wrangler pages deploy xpertinformatica --project-name xpertinformatica-website",
    "deploy:xpertinformatica-route": "npx wrangler deploy --config wrangler.xpertinformatica.jsonc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@esbuild/linux-x64": "^0.28.2",
    "esbuild": "^0.28.1",
    "ssh2": "^1.17.0"
  }
}
